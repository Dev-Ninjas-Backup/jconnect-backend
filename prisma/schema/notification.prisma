model Notification {
  id String @id @default(uuid())

  // User receiving notification
  userId String
  user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)

  title    String
  message  String  @db.Text
  entityId String?
  read     Boolean @default(false)

  // Additional data as JSON
  metadata  Json?
  users     UserNotification[]
  createdAt DateTime           @default(now())
  updatedAt DateTime           @updatedAt

  @@index([userId])
  @@index([read])
  @@index([createdAt])
  @@map("notifications")
}

model UserNotification {
  id String @id @default(uuid())

  user           User              @relation(fields: [userId], references: [id], onDelete: Cascade)
  userId         String
  type           NotificationType?
  notification   Notification      @relation(fields: [notificationId], references: [id], onDelete: Cascade)
  notificationId String

  read Boolean @default(false)

  createdAt DateTime @default(now())
  updatedAt DateTime @default(now()) @updatedAt

  @@unique([userId, notificationId])
}

enum NotificationType {
  Service
  Payment
  UserRegistration
  Inquiry
  UploadProof
  PaymentReminder
  follow
  UPLOAD_PROOF
  SERVICE_REQUEST_ACCEPTED
  SERVICE_REQUEST_REJECTED
  SERVICE_REQUEST_CANCELLED
  PAYMENT_SUCCESSFUL
  PAYMENT_FAILED
  INQUIRY_RESPONSE
  REVIEW_RECEIVED
  POST_LIKED
  POST_COMMENTED
  POST_SHARED
  POST_REPLIED

  // Repost - Follow
  FOLLOWED_SELLER_NEW_LISTING

  // Repost - Buyer
  REPOST_ORDER_SUBMITTED
  REPOST_SELLER_ACCEPTED
  REPOST_SELLER_REJECTED
  REPOST_PROOF_SUBMITTED
  REPOST_REVIEW_WINDOW_STARTED
  REPOST_REDO_SUBMITTED
  REPOST_FUNDS_RELEASED
  REPOST_DISPUTE_UPDATED

  // Repost - Seller
  REPOST_NEW_REQUEST
  REPOST_EXPIRING_SOON
  REPOST_REQUEST_ACCEPTED
  REPOST_PROOF_SENT
  REPOST_REDO_REQUESTED
  REPOST_SELLER_FUNDS_RELEASED
  REPOST_DISPUTE_OPENED

  // Escrow
  ESCROW_FUNDS_HELD
  ESCROW_REFUND_ISSUED
  ESCROW_FUNDS_RELEASED
  ESCROW_FUNDS_PENDING
  ESCROW_REFUND_PROCESSED

  // Listing Management
  LISTING_APPROVED
  LISTING_PAUSED
  LISTING_REACTIVATED
  LISTING_REMOVED
  LISTING_FEATURED

  // Performance
  NEW_REVIEW_RECEIVED
  RATING_INCREASED
  RATING_DECREASED
  REPOST_MILESTONE
  EARNINGS_MILESTONE

  // System
  WELCOME
  PASSWORD_CHANGED
  LOGIN_DETECTED
  SECURITY_ALERT
  PROFILE_VERIFICATION_APPROVED
  PROFILE_VERIFICATION_REJECTED
}
